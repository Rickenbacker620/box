{
  "name": "box",
  "private": true,
  "scripts": {
    "deploy:frontend": "wrangler pages deploy box-frontend/dist --project-name=box-frontend",
    "deploy:worker": "cd box-worker && wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.94.0"
  }
}